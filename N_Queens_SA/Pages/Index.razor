@page "/"
@using N_Queens_SA.ComponenentsSolver.QueensXadrez

<QueensTabuleiro parameterSolver="parameterSolver"></QueensTabuleiro>

@code{

    public ParametersOfSolver parameterSolver { get; set; }

    protected async override Task OnInitializedAsync()
    {
        parameterSolver = new ParametersOfSolver(4, 35.0, 35.0, 0.05, 1.005, 0.0);
        base.OnInitializedAsync();
    }
}
